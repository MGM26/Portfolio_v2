tailwind install :

#Version 4 deprecated init to reduce file count
npm install -D tailwindcss postcss autoprefixer
npx tailwindcss init -p

#version 3:
npm install -D tailwindcss@3 postcss autoprefixer
npx tailwindcss init -p

#tailwind cnfg:
export default {
  content: ["./index.html", "./src/**/*.{js,ts,jsx,tsx}"],
  theme: {
    extend: {},
  },
  plugins: [],
};


@tailwind base;
@tailwind components;
@tailwind utilities;

delete app.css


npm install tailwind-scrollbar

 npm install tailwind-scrollbar@3


 Proptypes:

 to justify the parameters for the components so as to typecaste and non null conditions 
 \

 lenis react smooth scrolling
 